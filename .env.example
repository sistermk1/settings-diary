# Google OAuth client ID (Phase 2+). Public value — safe to expose in the frontend.
# Create at https://console.cloud.google.com → 認証情報 → OAuth クライアント ID (ウェブアプリケーション).
# Phase 1 is local-only and does NOT require this.
VITE_GOOGLE_CLIENT_ID=
